<h1>Sign Up</h1>

<%= form_with model: @user, url: registration_path do |form| %>
  <% if form.object.errors.any? %>
    <% form.object.errors.full_messages.each do |message| %>
      <div><%= message %></div>
    <% end %>
  <% end %>

  <div>
    <%= form.lable :email %>
    <%= form.email_field :email %>
  </div>

  <div>
    <%= form.lable :password %>
    <%= form.password_field :password %>
  </div>

  <div>
    <%= form.lable :password_confirmation %>
    <%= form.password_confirmation_field :password_confirmation %>
  </div>
  
  <div>
    <%= form.submit "Sign Up" %>
  </div>
<% end %>