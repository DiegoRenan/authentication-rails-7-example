name: Create Release
on:
  push:
    branches: [main]
jobs:
  bump-version:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v4
      - name: GitRef
        run: "${{ toJSON(github) }}"
